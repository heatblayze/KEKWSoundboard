<local:PopupPage x:Class="KEKWSoundboard.Pages.NewItemPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KEKWSoundboard.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NewItemPage" Loaded="PopupPage_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Style="{StaticResource NormalButton}" Content="Sound" HorizontalAlignment="Center" VerticalAlignment="Center" Height="430" Width="380" FontSize="36" Click="SoundButton_Click" />
        <Button x:Name="folderButton" Grid.Column="1" Style="{StaticResource NormalButton}" Content="Folder" HorizontalAlignment="Center" VerticalAlignment="Center" Height="430" Width="380" FontSize="36" Click="FolderButton_Click"/>

    </Grid>
</local:PopupPage>
